// Sanity CDN transform params applied to photographic image URLs so the
// origin serves a resized/optimized derivative instead of the full upload.
export const IMAGE_PARAMS = '?w=1600&auto=format&q=82'
